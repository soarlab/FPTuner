;; -*- mode: scheme -*-

(FPCore (x eps)
        :name "nmse_example_3_3"
        :cite (hamming-1987 herbie-2015)
        :fpbench-domain textbook
        :pre (and (<= 0 x 10)
                  (<= 0 eps 1/128))
        (- (sin (+ x eps)) (sin x)))
