;; -*- mode: scheme -*-

(FPCore (x eps)
        :name "nmse_problem_3_3_5"
        :cite (hamming-1987 herbie-2015)
        :fpbench-domain textbook
        :pre (and (<= 0 x 10)
                  (<= 0 eps 1/128))
        (- (cos (+ x eps)) (cos x)))
